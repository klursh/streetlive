<%= simple_form_for shallow_args(message.artist, message), html: { class: "form-horizontal" } do |f| %>
<%= f.input :content %>
<%= f.submit t("messages.submit", default: "Submit"), class: "btn btn-primary" %>
<% end %>
